[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Fallback for React router / client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
